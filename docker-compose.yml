services:
  # Main Application Service (what Coolify will deploy)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: atoz-bot-app
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://atoz_user:atoz_password@database:5432/atoz_bot_db}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - PYTHONPATH=/app/backend
      - ENVIRONMENT=production
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - HEALTH_CHECK_URL=http://localhost:8000/health
    volumes:
      - ./bot:/app/bot
      - ./logs:/app/logs
    ports:
      - "8000:8000"
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    deploy:
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
    networks:
      - atoz-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Frontend Service
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:8000"  # Map host port 3000 to container port 8000
    depends_on:
      - app
    networks:
      - atoz-network
    restart: unless-stopped

  # PostgreSQL Database
  database:
    image: postgres:15-alpine
    container_name: atoz-database
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-atoz_bot_db}
      POSTGRES_USER: ${POSTGRES_USER:-atoz_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-atoz_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "5432:5432"  # Use port 5433 on host to avoid conflict with local PostgreSQL
    networks:
      - atoz-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-atoz_user} -d ${POSTGRES_DB:-atoz_bot_db}"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: atoz-redis
    ports:
      - "6380:6379"  # Use port 6380 on host to avoid conflict with local Redis
    volumes:
      - redis_data:/data
    networks:
      - atoz-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  atoz-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
